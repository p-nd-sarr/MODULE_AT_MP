<li class="<%= 'active' if is_active_controller('admin/dossier_juridiques') %>">
  <a href="#"><i class="fa fa-folder"></i>
    <span class="nav-label">Gestion affaires juridiques</span><span class="fa arrow"></span></a>
  <ul class="nav nav-second-level <%= is_active_controller('admin/dossier_juridiques') %>">
    <li class="<%= 'active' if is_active_controller('admin/dossier_juridiques') and is_active_action('index') %>">
      <%= link_to 'Accueil', admin_dossier_juridiques_path %>
    </li>
    <li class="<%= 'active' if is_active_controller('admin/dossier_juridiques') and is_active_action('new') %>">
      <%= link_to "Ajouter un dossier", new_admin_dossier_juridique_path %>
    </li>
  </ul>
</li>
<li class="<%= 'active' if is_active_controller('admin/liquidation_retraites') %>">
  <a href="#"><i class="fa fa-users"></i>
    <span class="nav-label">Liquidation Retraite</span><span class="fa arrow"></span></a>
  <ul class="nav nav-second-level <%= is_active_controller('admin/liquidation_retraites') %>">
    <li class="<%= 'active' if is_active_controller('admin/liquidation_retraites') and is_active_action('index') %>">
      <%= link_to 'Toutes les demandes', admin_liquidation_retraites_path %>
    </li>
  </ul>
</li>

<li class="<%= 'active' if is_active_controller('admin/allocataires') %>">
  <a href="#"><i class="fa fa-users"></i>
    <span class="nav-label">Allocataires</span><span class="fa arrow"></span></a>
  <ul class="nav nav-second-level <%= is_active_controller('admin/allocataires') %>">
    <li class="<%= 'active' if is_active_controller('admin/allocataires') and is_active_action('index') %>">
      <%= link_to 'Liste des allocataires', admin_allocataires_path %>
    </li>
  </ul>
</li>

