wb = xlsx_package.workbook

wb.add_worksheet(name: 'liste') do |sheet|
  titre = [
      'Numéro dossier',
      'Nom',
      'Prénom',
      'Sexe',
      'Téléphone',
      'Email',
      'Etat du dossier',
      'Agence de gestion',
      'Mode de paiement',
      'Gestionnaire qui a ouvert',
      'Date d''ouverture',
      'Gestionnaire compte allocataire',
      'Gestionnaire compte salarié',
  ]
  sheet.add_row titre, :b => true
  @demande_liquidations_export.each do |t|
    sheet.add_row [
                      t.num_dossier,
                      t.nom,
                      t.prenom,
                      t.sexe_human,
                      t.telephone,
                      t.email,
                      LiquidationRetraite.workflow_spec.states[t.workflow_state.to_sym].meta[:label],
                      t.agence_creation.try(:description_prest),
                      t.mode_paiement_human,
                      t.ajoute_par.try(:full_name),
                      t.date_ouverture,
                      t.affecter_allocataire.try(:full_name),
                      t.affecter_salarie.try(:full_name),
                  ]
  end
end
