<div class="modal inmodal" id="SoumissionModal" tabindex="-1" role="dialog" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content animated bounceInRight">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal"><span aria-hidden="true">&times;</span><span class="sr-only">Close</span></button>
        <i class="fa fa-check modal-icon btn-primary"></i>
        <h4 class="modal-title">
          soumission d'une demande de reversion
        </h4>

      </div>
      <div class="modal-body">
       <%= bootstrap_form_for(@reversion_veuve_salarie, url: admin_base_reversion_salary_soumettre_action_path(@reversion_veuve_salarie)) do |f| %>
        <table class="table table-condensed table-hover">
         
          <tr>
            <td><input type="checkbox" name="vehicle1" value="Bike">
            </td>
            <td><strong>Condition 1 : Je déclare que le défunt n'a plus exercé d'activité salariée à compter du <%= l @reversion_veuve_salarie.date_cessation_activite %> </strong></td>
          </tr>
            <tr>

              <td> <input type="checkbox" name="vehicle1" value="Bike">
              </td>

              <td><strong>Condition 3 : Je certifie que les renseignements portés sur la présente demande sont exacts.</strong></td>
            </tr>
        </table>

        <div class="modal-body">
          <%= f.text_area :commentaire_soumission, label: 'Commentaire' %>
        </div>
      
      </div> 
      <div class="modal-footer">
        <button type="button" class="btn btn-white" data-dismiss="modal">Fermer</button>
        <%= f.button 'Valider', class: 'btn btn-primary', id: 'soummetre_valide' %>
      </div>

      <% end %>
    </div>
  </div>
</div>

<script>

    $(document).ready(function(){

        $('input:checkbox').removeAttr('checked');

        $("#soummetre_valide").hide();
        var all_checkboxes = $('#SoumissionModal input[type="checkbox"]');

        $(".modal-body").change(function(){

            if (all_checkboxes.length === all_checkboxes.filter(":checked").length) {
                $("#soummetre_valide").show();
            }else{
                $("#soummetre_valide").hide();

            }

        });

    });

</script>

