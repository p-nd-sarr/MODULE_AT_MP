<%= content_for :titre_page, 'Admin Villes' %>

<%= content_for :actions_page do %>
  <%= link_to 'Ajouter', new_admin_ville_path, class: 'btn btn-primary btn-sm' %>
<% end %>

<div class="wrapper wrapper-content">
  <div class="ibox-content">
    <table class="table table-bordered table-condensed table-hover">
      <thead>
      <tr>
        <th scope="col">Admin departement</th>
        <th scope="col">Code</th>
        <th scope="col">Designation</th>
        <th colspan="3"></th>
      </tr>
      </thead>

      <tbody>
      <% @admin_villes.each do |admin_ville| %>
        <tr>
          <td><%= admin_ville.admin_departement.designation %></td>
          <td><%= admin_ville.code %></td>
          <td><%= admin_ville.designation %></td>
          <td style="width: 75px">
            <%= link_to 'détails', admin_ville, class: 'btn btn-outline btn-primary btn-xs' %>
          </td>
          <td style="width: 75px">
            <%= link_to 'modifier', edit_admin_ville_path(admin_ville), class: 'btn btn-outline btn-primary btn-xs' %>
          </td>
          <td style="width: 75px">
            <%= link_to 'supprimer', admin_ville, method: :delete, data: {confirm: 'En êtes vous sûr?'}, class: 'btn btn-outline btn-danger btn-xs' %>
          </td>
        </tr>
      <% end %>
      </tbody>
    </table>
  </div>
</div>