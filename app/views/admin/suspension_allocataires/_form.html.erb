
<%= bootstrap_form_for([:admin, @allocataire, @suspension_allocataire], layout: :horizontal, label_col: "col-sm-4", control_col: "col-sm-8") do |f| %>
  
  <%= f.hidden_field :etat , class:'form-control', value:2 %>
      <div class="form-group">
    <%= f.text_area :motif_suspension , class:'form-control' %>

  <div class="hr-line-dashed"></div>

  <div class="form-group row">
    <div class="col-sm-4 col-sm-offset-4">
      <%= link_to 'Annuler', (@suspension_allocataire.persisted? ? [:admin, @allocataire, @suspension_allocataire] : admin_allocataire_suspension_allocataires_path(@allocataire)), class: 'btn btn-white' %>
      <%= f.button 'Valider', class: 'btn btn-primary' %>
    </div>
  </div>
<% end %>
