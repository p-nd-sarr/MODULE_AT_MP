<%= content_for :titre_page, 'Échéances veuves caisse' %>

<%= content_for :actions_page do %>

<% end %>

<div class="wrapper wrapper-content">
  <div class="ibox-content">
    <%= paginate @echeances %>

    <table class="table table-bordered table-condensed table-hover">
      <thead>
      <tr>
        <th scope="col" style="width: 10px">#</th>
        <th scope="col">Date génération</th>
        <th scope="col">Statut</th>
        <th scope="col">Période</th>
        <th colspan="1"></th>
      </tr>
      </thead>

      <tbody>
      <% @echeances.each do |echeance| %>
        <tr>
          <td><%= echeance.id %></td>
          <td><%= l echeance.created_at %></td>
          <td><%= EcheanceVeuvesCaisse.workflow_spec.states[echeance.workflow_state.to_sym].meta[:label] %></td>
          <td><%= l echeance.periode_debut %> - <%= l echeance.periode_fin %></td>
          <td style="width: 75px">
            <%= link_to 'détails', [:admin, echeance], class: 'btn btn-outline btn-primary btn-xs' %>
          </td>
        </tr>
      <% end %>
      </tbody>
    </table>

    <%= paginate @echeances %>
  </div>
</div>