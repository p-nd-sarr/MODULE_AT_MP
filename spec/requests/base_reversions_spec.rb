 require 'rails_helper'

# This spec was generated by rspec-rails when you ran the scaffold generator.
# It demonstrates how one might use RSpec to test the controller code that
# was generated by Rails when you ran the scaffold generator.
#
# It assumes that the implementation code is generated by the rails scaffold
# generator. If you are using any extension libraries to generate different
# controller code, this generated spec may or may not pass.
#
# It only uses APIs available in rails and/or rspec-rails. There are a number
# of tools you can use to make these specs even more expressive, but we're
# sticking to rails and rspec-rails APIs to keep things simple and stable.

RSpec.describe "/base_reversions", type: :request do
  # BaseReversion. As you add validations to BaseReversion, be sure to
  # adjust the attributes here as well.
  let(:valid_attributes) {
    skip("Add a hash of attributes valid for your model")
  }

  let(:invalid_attributes) {
    skip("Add a hash of attributes invalid for your model")
  }

  describe "GET /index" do
    it "renders a successful response" do
      BaseReversion.create! valid_attributes
      get base_reversions_url
      expect(response).to be_successful
    end
  end

  describe "GET /show" do
    it "renders a successful response" do
      base_reversion = BaseReversion.create! valid_attributes
      get base_reversion_url(base_reversion)
      expect(response).to be_successful
    end
  end

  describe "GET /new" do
    it "renders a successful response" do
      get new_base_reversion_url
      expect(response).to be_successful
    end
  end

  describe "GET /edit" do
    it "render a successful response" do
      base_reversion = BaseReversion.create! valid_attributes
      get edit_base_reversion_url(base_reversion)
      expect(response).to be_successful
    end
  end

  describe "POST /create" do
    context "with valid parameters" do
      it "creates a new BaseReversion" do
        expect {
          post base_reversions_url, params: { base_reversion: valid_attributes }
        }.to change(BaseReversion, :count).by(1)
      end

      it "redirects to the created base_reversion" do
        post base_reversions_url, params: { base_reversion: valid_attributes }
        expect(response).to redirect_to(base_reversion_url(BaseReversion.last))
      end
    end

    context "with invalid parameters" do
      it "does not create a new BaseReversion" do
        expect {
          post base_reversions_url, params: { base_reversion: invalid_attributes }
        }.to change(BaseReversion, :count).by(0)
      end

      it "renders a successful response (i.e. to display the 'new' template)" do
        post base_reversions_url, params: { base_reversion: invalid_attributes }
        expect(response).to be_successful
      end
    end
  end

  describe "PATCH /update" do
    context "with valid parameters" do
      let(:new_attributes) {
        skip("Add a hash of attributes valid for your model")
      }

      it "updates the requested base_reversion" do
        base_reversion = BaseReversion.create! valid_attributes
        patch base_reversion_url(base_reversion), params: { base_reversion: new_attributes }
        base_reversion.reload
        skip("Add assertions for updated state")
      end

      it "redirects to the base_reversion" do
        base_reversion = BaseReversion.create! valid_attributes
        patch base_reversion_url(base_reversion), params: { base_reversion: new_attributes }
        base_reversion.reload
        expect(response).to redirect_to(base_reversion_url(base_reversion))
      end
    end

    context "with invalid parameters" do
      it "renders a successful response (i.e. to display the 'edit' template)" do
        base_reversion = BaseReversion.create! valid_attributes
        patch base_reversion_url(base_reversion), params: { base_reversion: invalid_attributes }
        expect(response).to be_successful
      end
    end
  end

  describe "DELETE /destroy" do
    it "destroys the requested base_reversion" do
      base_reversion = BaseReversion.create! valid_attributes
      expect {
        delete base_reversion_url(base_reversion)
      }.to change(BaseReversion, :count).by(-1)
    end

    it "redirects to the base_reversions list" do
      base_reversion = BaseReversion.create! valid_attributes
      delete base_reversion_url(base_reversion)
      expect(response).to redirect_to(base_reversions_url)
    end
  end
end
